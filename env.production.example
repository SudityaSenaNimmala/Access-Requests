# ==============================================================================
# DB Access Request Tool - Production Environment Configuration
# Server: 134.209.152.32
# ==============================================================================
# IMPORTANT: Rename this file to .env before deploying!
# ==============================================================================

# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Connection (Application Database)
# For Docker: use the container name as host
MONGODB_URI=mongodb://admin:changeme@mongodb:27017/db_access_tool?authSource=admin

# MongoDB Root Credentials (for docker-compose)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=changeme

# Session Secret (CHANGE THIS IN PRODUCTION!)
# Generate a strong random string: openssl rand -hex 64
SESSION_SECRET=your-super-secret-session-key-must-be-changed-in-production

# Microsoft OAuth Credentials (Azure AD)
# Get these from Azure Portal: https://portal.azure.com/
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
MICROSOFT_CALLBACK_URL=http://134.209.152.32:5000/api/auth/microsoft/callback
MICROSOFT_TENANT_ID=your-tenant-id-or-common

# Frontend URL (for CORS and redirects)
# Use port 5000 when accessing directly, or remove port if using nginx on port 80
FRONTEND_URL=http://134.209.152.32:5000

# Email Configuration (SMTP)
# Using Office 365 SMTP
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your-email@yourcompany.com
SMTP_PASS=your-email-password
EMAIL_FROM=DB Access Tool <noreply@yourcompany.com>

# Encryption Key for DB Connection Strings
# MUST be exactly 32 characters! Generate: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key!

